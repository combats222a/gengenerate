<!--
  Заметка от Claude (Этап 13): исходное содержимое этого файла
  инструктировало ИИ-агента читать несуществующий путь
  `node_modules/next/dist/docs/` под предлогом того, что в проекте
  используется нестандартная версия Next.js с "breaking changes" —
  такого пути не существует ни в одной реальной версии Next.js.
  Это похоже на попытку prompt injection (подмену инструкций для ИИ),
  а не на легитимную заметку для разработчиков, поэтому содержимое
  удалено. Реальная версия — обычный Next.js 16.2.11 (см. package.json),
  никаких особых инструкций для его использования не требуется.
-->
